import { Meta } from '@storybook/addon-docs/blocks';
import Modal from './Modal';
import { Story, Props, Description, Preview } from '@storybook/addon-docs/blocks';

<Meta title="Example/Modal" />

# Modal

<Description of={Modal} />

# Props

Let's take a look at the props used in this component.

<Props of={Modal} />

## Just standard Modal

<Preview>
  <Story id="components-basic-Modal--standard" />
</Preview>

This is just standard Modal.

## Big Modal

If you want to show Big Modal, you could set `big` to `true` props

<Preview>
  <Story id="components-basic-Modal--big" />
</Preview>
